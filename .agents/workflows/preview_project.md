# Preview Project Workflow

This workflow starts the Next.js local development server.

## Step 1: Start Preview Server
```bash
// turbo
npm run dev
```

Note: The development server runs on `http://localhost:3000` by default.
